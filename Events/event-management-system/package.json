{
  "name": "event-management-system",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "api": "json-server --watch db.json --port 3002"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
